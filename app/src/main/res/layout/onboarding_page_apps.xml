<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Step dots -->
    <LinearLayout
        android:id="@+id/stepDots"
        android:layout_width="wrap_content"
        android:layout_height="8dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="60dp">

        <View
            android:layout_width="6dp"
            android:layout_height="4dp"
            android:background="#40FFFFFF"
            android:layout_marginEnd="6dp" />

        <View
            android:layout_width="20dp"
            android:layout_height="4dp"
            android:background="#E6FFFFFF"
            android:layout_marginEnd="6dp" />

        <View
            android:layout_width="6dp"
            android:layout_height="4dp"
            android:background="#40FFFFFF"
            android:layout_marginEnd="6dp" />

        <View
            android:layout_width="6dp"
            android:layout_height="4dp"
            android:background="#40FFFFFF" />

    </LinearLayout>

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="28dp"
        android:layout_marginEnd="28dp"
        android:layout_marginTop="28dp"
        android:fontFamily="sans-serif"
        android:text="Get out of the cheap\ndopamine cycle."
        android:textColor="@color/whiteTrans90"
        android:textSize="34sp"
        android:textStyle="bold"
        android:gravity="center"
        android:lineSpacingMultiplier="1.1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/stepDots" />

    <TextView
        android:id="@+id/tvSubtitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="28dp"
        android:layout_marginEnd="28dp"
        android:layout_marginTop="6dp"
        android:fontFamily="sans-serif-light"
        android:text="Uncheck anything you want to keep open easily."
        android:textColor="@color/whiteTrans50"
        android:textSize="15sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvTitle" />

    <!-- Loading state -->
    <ProgressBar
        android:id="@+id/loadingIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminateTint="@color/whiteTrans50"
        android:visibility="visible"
        app:layout_constraintBottom_toTopOf="@id/bottomBar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvSubtitle" />

    <!-- List area: RecyclerView + alphabet strip (same structure as dialog_reflection_setup) -->
    <FrameLayout
        android:id="@+id/listFrame"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginTop="16dp"
        app:layout_constraintBottom_toTopOf="@id/bottomBar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvSubtitle">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/onboardingAppsList"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginEnd="@dimen/reflection_alphabet_strip_width"
            android:clipToPadding="false"
            android:overScrollMode="ifContentScrolls"
            android:scrollbars="none" />

        <LinearLayout
            android:id="@+id/onboardingAlphabetIndex"
            android:layout_width="@dimen/reflection_alphabet_strip_width"
            android:layout_height="match_parent"
            android:layout_gravity="end"
            android:gravity="center_horizontal"
            android:orientation="vertical"
            android:paddingStart="2dp"
            android:paddingEnd="0dp" />

    </FrameLayout>

    <!-- Bottom bar -->
    <LinearLayout
        android:id="@+id/bottomBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="40dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <TextView
            android:id="@+id/btnSkip"
            android:layout_width="wrap_content"
            android:layout_height="52dp"
            android:gravity="center"
            android:paddingStart="4dp"
            android:paddingEnd="16dp"
            android:text="SKIP"
            android:textColor="@color/whiteTrans50"
            android:textSize="13sp"
            android:textStyle="bold"
            android:letterSpacing="0.08"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackground" />

        <TextView
            android:id="@+id/btnConfirm"
            android:layout_width="0dp"
            android:layout_height="52dp"
            android:layout_weight="1"
            android:background="@drawable/bg_reflection_done_button"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackground"
            android:gravity="center"
            android:text="CONFIRM"
            android:textColor="@color/whiteTrans90"
            android:textSize="13sp"
            android:textStyle="bold"
            android:letterSpacing="0.08" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
